name: Check forbidden paths

on:
  push:
  pull_request:

jobs:
  check-forbidden-paths:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Detect forbidden paths
        run: |
          echo "🔍 Checking for forbidden paths..."
          if git ls-files | grep -qE '\.terraform/'; then
            echo "🚫 Forbidden Terraform cache files found:"
            git ls-files | grep -E '\.terraform/'
            echo "Please remove .terraform/ folders and re-commit."
            exit 1
          else
            echo "✅ No forbidden paths found."
          fi
