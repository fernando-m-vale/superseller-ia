## Contexto
Preparar a API Fastify para o MVP com endpoint de saúde, CORS habilitado para `http://localhost:3000`, logs estruturados e lint configurado.

## Tarefas
- [ ] Garantir scripts do `apps/api/package.json` (`dev`, `build`, `start`, `lint`)
- [ ] Endpoint `GET /health` retornando `{ "status": "ok" }`
- [ ] CORS liberado para origem configurada por env (`CORS_ORIGIN`)
- [ ] Logs estruturados (json) ativados no Fastify
- [ ] `.env.example` atualizado (PORT, CORS_ORIGIN, JWT_SECRET)
- [ ] CI rodando build da API

## Critérios de Aceite (AC)
- [ ] `pnpm --filter api dev` sobe a API localmente sem erro
- [ ] `GET http://localhost:3001/health` → 200 + `{ "status": "ok" }`
- [ ] CORS aceitando `http://localhost:3000`
- [ ] Lint/Typecheck ok (CI verde)

## Definition of Done (DoD)
- [ ] PR pequeno, commits padronizados (Conventional Commits)
- [ ] Descrição do PR com o que foi feito e instruções de teste
- [ ] Sem segredos no repo; `.env.example` atualizado

## Riscos
- Configuração CORS permissiva demais (mitigar com env)
- Conflitos de portas locais

## Links
- `docs/README-dev.md` (setup dev)
- `docs/architecture.md` (contratos e visão)
